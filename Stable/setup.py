# A placeholder when able to create a real setup.
# This just generates compiled python code (.pyc)
# i.e it runs without python.exe console instance, but still depends on the interpreter

import py_compile

py_compile.compile("LL.py")
